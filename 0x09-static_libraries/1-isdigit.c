#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isdigit - Returns True if all the characters are digits, otherwise False
 * @c: Character to check
 * Return: Nothing.
 */

int _isdigit(int c)
{
return (isdigit(c));
}
