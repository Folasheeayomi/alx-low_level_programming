#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _abs - Returns the absolute value of a number
 * @n: The number whose absolute value is returned
 * Return: Integer value
 */

int _abs(int n)
{
return (abs(n));
}
